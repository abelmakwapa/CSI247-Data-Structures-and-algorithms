import { NO_OUTPUT, type CodeExampleConfig } from './types';

export const linkedListsCodeExample = {
  testCases: [{ id: 'default', label: 'Build the linked nodes', expectedOutput: { javascript: NO_OUTPUT, python: NO_OUTPUT } }],
} satisfies CodeExampleConfig;
