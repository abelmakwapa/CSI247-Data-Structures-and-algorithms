import type { CodeExampleConfig } from './types';

export const stacksCodeExample = {
  testCases: [{ id: 'default', label: 'Pop the newest item', expectedOutput: { javascript: 'parse body', python: 'parse body' } }],
} satisfies CodeExampleConfig;
