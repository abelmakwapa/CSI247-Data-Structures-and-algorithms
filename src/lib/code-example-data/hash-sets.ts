import type { CodeExampleConfig } from './types';

export const hashSetsCodeExample = {
  testCases: [{ id: 'default', label: 'Count unique values', expectedOutput: { javascript: '2', python: '2' } }],
} satisfies CodeExampleConfig;
