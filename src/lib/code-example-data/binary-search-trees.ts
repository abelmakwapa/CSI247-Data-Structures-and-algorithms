import { NO_OUTPUT, type CodeExampleConfig } from './types';

export const binarySearchTreesCodeExample = {
  testCases: [{ id: 'default', label: 'Define the search function', expectedOutput: { javascript: NO_OUTPUT, python: NO_OUTPUT } }],
} satisfies CodeExampleConfig;
