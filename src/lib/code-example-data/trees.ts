import type { CodeExampleConfig } from './types';

export const treesCodeExample = {
  testCases: [{ id: 'default', label: 'Read the child node', expectedOutput: { javascript: 'index.js', python: 'index.py' } }],
} satisfies CodeExampleConfig;
