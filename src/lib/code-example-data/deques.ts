import type { CodeExampleConfig } from './types';

export const dequesCodeExample = {
  testCases: [{ id: 'default', label: 'Remove from the back', expectedOutput: { javascript: 'C', python: 'C' } }],
} satisfies CodeExampleConfig;
