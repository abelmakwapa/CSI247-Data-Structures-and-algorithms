import type { CodeExampleConfig } from './types';

export const disjointSetUnionCodeExample = {
  testCases: [{ id: 'default', label: 'Find the representative', expectedOutput: { javascript: '0', python: '0' } }],
} satisfies CodeExampleConfig;
